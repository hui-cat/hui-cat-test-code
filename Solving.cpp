//使用二分法解方程
#include <iostream>

using namespace std;

int main()
{
    int a,b,c,d;
    cin >> a >> b >> c >> d;
    for (size_t i = -100; i < 100; i++)
    {
        int j = i+1;
        if ((a*i*i*i+b*i*i+c*i+d)*(a*j*j*j+b*j*j+c*j+d)<=0)
        {
            
        }
    }
    
}